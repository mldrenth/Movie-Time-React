name: Run Unit Tests
on: push
jobs:
  test:
    name: Test
    runs-on: macos-latest
    steps:
      - name: Install Dependencies
        run: npm install
      - name: Run unit tests
        run: npm run test

        